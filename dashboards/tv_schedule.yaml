# 📺 TV Schedule Dashboard View
# Shows tonight's TV lineup and upcoming episodes
# 
# SETUP REQUIRED:
# 1. Install "ICS Calendar" via HACS
# 2. Add Integration: Settings → Devices & Services → ICS Calendar
# 3. Enter URL: https://tvcal.app/ical/326blc
# 4. Once setup, a calendar.tv_schedule entity will be available
# 5. Uncomment the calendar card below after setup

title: "📺 TV Schedule"
path: tv-schedule
icon: mdi:television
type: sections
max_columns: 3

sections:
  - type: grid
    cards:
      # Tonight's Schedule
      - type: custom:upcoming-media-card
        entity: sensor.tonight_tv_schedule
        title: "Tonight's TV"
        image_style: fanart
        line1: $title
        line2: $episode
        line3: $day - $time
        line4: $rating
        max: 5

      # Upcoming Episodes
      - type: custom:upcoming-media-card
        entity: sensor.upcoming_episodes
        title: "Upcoming Episodes"
        image_style: poster
        line1: $title
        line2: $episode
        line3: $day - $time
        line4: $number - $rating
        max: 10

      # Show Status
      - type: entities
        title: "TV Status"
        entities:
          - entity: sensor.tonight_tv_schedule
          - entity: sensor.upcoming_episodes
          - entity: binary_sensor.new_episode_available

      # Quick Actions
      - type: entities
        title: "Quick Actions"
        entities:
          - entity: automation.notify_new_episodes
            name: "Episode Notifications"
          - entity: automation.daily_tv_schedule_summary
            name: "Daily Summary"

  - type: grid
    cards:
      # TV Schedule Calendar - from https://tvcal.app/ical/326blc
      # After setting up ICS Calendar integration, use one of these options:
      
      # Option 1: Full Calendar View (Recommended)
      # Uncomment when calendar.tv_schedule entity exists
      # - type: calendar
      #   entities:
      #     - calendar.tv_schedule
      #   title: "TV Schedule Calendar"
      
      # Option 2: Simple List View
      # Uncomment when calendar.tv_schedule entity exists
      # - type: entities
      #   title: "TV Schedule"
      #   entities:
      #     - entity: calendar.tv_schedule
      
      # Temporary placeholder card (remove after calendar setup)
      - type: markdown
        content: |
          ## 📺 TV Schedule Setup Required
          
          **Step 1:** Install ICS Calendar via HACS
          
          **Step 2:** Add Integration
          - Go to: Settings → Devices & Services
          - Add Integration: ICS Calendar
          - URL: `https://tvcal.app/ical/326blc`
          - Name: TV Schedule
          
          **Step 3:** Uncomment calendar card above
          
          See `APPLE_TV_TV_SCHEDULE_SETUP.md` for details
      
      # Specific Shows - Temporarily commented out until TV integrations are configured
      # - type: custom:upcoming-media-card
      #   entity: calendar.the_traitors
      #   title: "The Traitors"
      #   max: 3

      # - type: custom:upcoming-media-card
      #   entity: calendar.the_night_manager
      #   title: "The Night Manager"
      #   max: 3

      # - type: custom:upcoming-media-card
      #   entity: calendar.happy_valley
      #   title: "Happy Valley"
      #   max: 3